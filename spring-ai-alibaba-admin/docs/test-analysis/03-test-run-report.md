# Test run report

_Generated by `/test-coverage-audit` on 2026-05-17. This file regenerates every run; only `Duration` is expected to vary on unchanged repos._

## Command

```
mvn -B -q -Dsurefire.printSummary=true test
```

(run in `spring-ai-alibaba-admin`, with `JAVA_HOME` set from `jenv prefix 17` — `openjdk version "17.0.19"`)

## Stats

- Tests run: **14**
- Passed: **14**
- Failed: **0**
- Errors: **0**
- Skipped: **0**
- Duration: **15.77s** wall-clock (Surefire `time` total: 1.33s; the rest is Maven reactor + compile)
- Exit code: **0**

Counts derived from the 7 Surefire XML reports in `spring-ai-alibaba-admin-server-core/target/surefire-reports/`:

| Test class | tests | failures | errors | skipped |
|---|---:|---:|---:|---:|
| `DashscopeRerankerTest` | varies | 0 | 0 | 0 |
| `DateUtilsTests` | varies | 0 | 0 | 0 |
| `KnowledgeBaseIndexPipelineTest` | varies | 0 | 0 | 0 |
| `PasswordCryptTest` | varies | 0 | 0 | 0 |
| `RSACryptTest` | varies | 0 | 0 | 0 |
| `TextDocumentReaderTest` | varies | 0 | 0 | 0 |
| `TextSplitterTest` | varies | 0 | 0 | 0 |
| **Total** | **14** | **0** | **0** | **0** |

## Failures

_None_

## Final verdict

✅ Suite green (14/14 unit tests passing under JDK 17)

### Notes

- The previous run failed with `BUILD FAILURE / TypeTag :: UNKNOWN` because Maven was invoked under JDK 25 (Homebrew Cellar) while Lombok 1.18.30 only supports up to JDK 21. Fix: project now ships a `.java-version` file pinning the tree to JDK 17 via [jenv](https://www.jenv.be/); the audit re-run used `JAVA_HOME="$(jenv prefix 17)"`.
- One stack trace shows up mid-log inside `DateUtilsTests.parseDateStringReturnsNullOnParseFailure` — this is a deliberate negative-path assertion (the test feeds `"invalid-date"` and asserts that `DateUtils.parseDateString` returns null while logging the `ParseException` at WARN). It is not a failure; the surrounding test still passed.
- This suite green verdict does **not** change the gap report: all 14 tests cover utility / RAG-helper classes (`PasswordCrypt`, `RSACrypt`, `DateUtils`, `TextDocumentReader`, `DashscopeReranker`, `KnowledgeBaseIndexPipeline`, `TextSplitter`), none of which are `*Controller` / `*Service` / `*ServiceImpl` / `*Mapper`. Coverage of the controller / service / mapper surface remains **0%**.
